<%= form_for(@organization, html: { class: 'small' }) do |f| %>
  <% if @organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% @organization.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :public_name %><br>
      <%= f.text_field :public_name, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :org_type %><br>
      <%= f.text_field :org_type, class: 'form-control' %>
    </div>

    <div class="form-group">
      <%= f.label :pricing_policy %><br>
      <%= f.text_field :pricing_policy, class: 'form-control' %>
    </div>

    <button type="submit" class="btn btn-primary">Submit</button>
    <%= link_to 'Cancel', organizations_path, class: "btn btn-default" %>
<% end %>
